{% extends 'base.html.twig' %}

{% block title %}DRAMATIQUE{% endblock %}

{% block body %}
    <h1>Change Password</h1>

    {{ form_start(form) }}
        <div class="form-group">{{ form_widget(form.plainPassword) }}</div>
        <div class="form-group">{{ form_widget(form.save) }}</div>
    {{ form_end(form) }}
{% endblock %}
